meta {
  name: Criar historico do pet
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/pets/{{petId}}/historico
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "descricao": "Alergia a determinado medicamento",
    "tipo": "ALERGIA"
  }
}
